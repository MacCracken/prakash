# Build artifacts
/target/
/build/

# Distlib companions — leaf-requirements manifests emitted alongside the
# tracked dist/*.cyr bundles by `cyrius distlib`; only the bundles are tracked.
/dist/prakash.deps
/dist/prakash-ai.deps

# Cyrius toolchain tarball (fetched during CI)
cyrius-*.tar.gz

# Rust reference archive (rust-old/) — Cargo build outputs, if ever built
Cargo.lock
rust-old/target/
rust-old/Cargo.lock

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Debugging
*.pdb
core
core.*

# Claude Code
.claude/

# Environment / secrets
.env
.env.*
