# Build artifacts
/target/
/build/

# Distlib companions — leaf-requirements manifests emitted alongside the
# tracked dist/*.cyr bundles by `cyrius distlib`; only the bundles are tracked.
/dist/prakash.deps
/dist/prakash-ai.deps

# Cyrius toolchain tarball (fetched during CI)
cyrius-*.tar.gz

# Library crate lock file
Cargo.lock

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Debugging
*.pdb
core
core.*

# Coverage
lcov.info
tarpaulin-report.html
coverage/

# Benchmark output (criterion generates large HTML reports)
criterion/

# Claude Code
.claude/

# Environment / secrets
.env
.env.*

# Property testing regressions
proptest-regressions/

# Supply chain cache
supply-chain/.cache/
