# cyrius dep sidecar - stdlib leaves this fold requires in scope.
# Maintained by scripts/sync-deps-sidecar.sh (NOT by `cyrius distlib` —
# see that script for why prakash overrides the generated sidecar).
syscalls
string
alloc
str
fmt
vec
io
args
assert
result
math
simd
ganita
tagged
fnptr
bench
callback
bayan
sakshi
